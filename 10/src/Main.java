public class Main {
    public static void main(String[] args) {
        IntEqualityPrinter.printEqual(1,2,3); //Variable cannot declared with method value as assign value because void is use and void
       // cannot return any value outside the method........
        // As nothing return outside the method so we cannot print ..
    }
}
